id: svn
name: svn
version: 1.2.0
extensions:
  - id: svn
    type: widget
    name: SVN
  - id: ttsinfo
    type: infobox
    name: TTS info for SVN
    schema:
      groups:
        - id: default
          fields:
            - id: textdata
              type: string
              title: Text Data